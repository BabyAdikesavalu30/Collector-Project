# Vigyaan — Final Bug Log

All bugs confirmed and fixed during the final frontend remediation pass.
Severity: **P0** = critical/unusable/security boundary, **P1** = major journey
broken, **P2** = significant UX/functionality, **P3** = minor.

**Remaining: P0 = 0 · P1 = 0 · P2 = 0 fixed · P3 = 0 newly found (documented
pre-existing items listed at the end).**

| ID | Sev | Feature / Screen | Reproduction | Expected | Actual | Root Cause | Fix | Regression Test | Status |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| BUG-001 | P1 | Concept Maps `/concept-map/[id]` | Open route with any id (valid, invalid, missing) | Loading spinner, then map — or a friendly bilingual not-found state with recovery | Blank screen (`return null`) while loading and permanently for unknown ids | Route bailed out on `!map` without handling `isLoading` or not-found | Added canonical loading state (`isLoading && !map`) and bilingual "Concept Map Not Found" state with Back-to-Hub CTA; mirrors the micro-lesson pattern | Static (tsc + export); pattern matches `MicroLessonDetailScreen` (existing tested behavior) | ✅ Fixed |
| BUG-002 | P1 | Spin Wheel `/spin-wheel` | Complete a spin; leave the screen / restart the app / re-enter the same day | One daily spin; state remembered | Wheel is re-spinnable the same day; earned points never persist to the XP ledger | `isDailyCompleted` was in-memory only; no storage key; no `recordXp` call anywhere in the flow | Added `@vigyaan/spin_wheel_state` + `spin-wheel.storage.ts` (validated reads, date-keyed), restored on mount, date-keyed `recordXp` award (`dedupeKey: spin-wheel-YYYY-MM-DD`), state wiped on logout | `spin-wheel.storage.test.ts` (7 tests: default, persist, same-day lock, next-day unlock, malformed JSON, wrong shape, idempotent re-mark) | ✅ Fixed |
| BUG-003 | P2 | Games Hub `/games`, header; Riddles `/riddles`, `/riddle-quiz` | Open Games or Riddles on a fresh account | Show the student's real points/balance (0 when none) | Hardcoded fake `1250` coin balance displayed | `points={1250}` literal + component defaults; riddle engine used `DEMO_RIDDLE_POINTS + score` as a fake base | Wired real `getXpSummary().totalXp` in routes; defaults changed to `0`; riddle quiz header shows the honest session score | Static (tsc + export); consistent with XP-ledger architecture covered by `xp.test.ts` | ✅ Fixed |
| BUG-004 | P3 | Games Hub Mystery Lab card | Switch UI to Tamil | Card title/subtitle localize | English-only card inside the Tamil app | Hardcoded strings instead of the existing `mysteryLab` i18n dict | Use `getTranslation(language).mysteryLab.title/subtitle` (`மர்ம ஆய்வகம்` …) | i18n parity suite (`i18n_parity.test.ts`) | ✅ Fixed |
| BUG-005 | P3 | Theme audit (5 sites in `app/`) | Inspect styles | Theme tokens only | Raw hex colors: `#FEF3C7`, `#FFFFFF`, `#FECACA`, `#FEF2F2`, `#FCA5A5` | Copy-paste hexes outside the design system | Mapped to `warningBackground`, `textOnAction`, `errorBorder`, `errorBackground` (values identical to canonical tokens) | Static (tsc) | ✅ Fixed |
| BUG-006 | P3 | Auth logout | Log out one student, log in another on the same device | New student has fresh daily spin | (Pre-existing list simply lacked the new key) | Logout wipe list predated `spin_wheel_state` | Added `SPIN_WHEEL_STATE` to the per-student logout wipe | Auth/demo session suite (`auth_demo_session.test.ts`) | ✅ Fixed |
| BUG-008 | P1 | Home Dashboard `/home`, `home2.service.ts` | Open Home Dashboard with 0 or multiple quiz attempts | Calculate actual practice accuracy honestly, or show "—" when no quizzes done | Evaluated `quizActivities.length > 0 ? 72 : 0`, inventing 72% | Hardcoded 72% placeholder in `buildProgressSnapshot` | Derived practice accuracy from canonical `quizStats.overallAccuracy` or average percentage across completed quiz activities; null when no quizzes; UI renders "—" | `home2.service.test.ts` (4 new tests verifying null, honest average, quizStats override, streak derivation) | ✅ Fixed |
| BUG-009 | P1 | Home Dashboard `home.service.ts` | Retrieve dashboard for student with active progress | Points, streak, and overall progress derived from real ledger | Base template kept hardcoded 840 points, 5 streak, 72% progress without overriding | Base template was populated from `MOCK_ACTIVE_DASHBOARD` without syncing user metrics | Overrode `baseData.points` with `totalXp`, `streakDays` with `StreakService.getUnifiedStreak()`, and `overallProgressPercentage` from actual activity count; honored `options?.mockEmpty` | `home.service.ts` + `tsc` + export suites | ✅ Fixed |
| BUG-010 | P1 | Profile Setup `/profile-complete`, `/profile-academic` | Open `/profile-complete` without prior session | Load actual profile or redirect to profile setup flow | Rendered fake student "Ananya Sharma", "R.M.K. Science Academy", "Chennai", and auto-synthesized an active session | Hardcoded fallback strings in `profile-complete.tsx` state & auto-session generation | Removed all fake identity fallbacks; loads real stored profile/session; redirects to `/profile-create` if unauthenticated; removed auto-session generation | `tsc` + `expo-doctor` + export suites | ✅ Fixed |
| BUG-011 | P1 | Certificate View `/certificate-view` | Navigate to `/certificate-view` with arbitrary query parameters | Resolve only canonical certificates by ID from storage, or show "Certificate not found" | Synthesized a fake certificate directly from route parameters (`params.recipientName || params.titleEn`) | Parameter fallback branch fabricated certificate records on the fly | Removed route parameter certificate synthesis; only resolves valid IDs via `getCertificateById(id)`, otherwise surfaces canonical not-found state with back navigation | `tsc` + export suites | ✅ Fixed |
| BUG-012 | P2 | Memory Matrix `/games/memory-matrix`, OTP `/otp` | Rapidly navigate away while mistake flip card or OTP resend toast timer is ticking | Clear active timer on unmount without warning | Potential state updates on unmounted component | `setTimeout` calls lacked unmount cleanup refs | Added `timeoutRef` and unmount cleanup in `useMemoryMatrixGame.ts` and `toastTimerRef` with unmount cleanup in `OtpVerificationScreen.tsx` | `tsc` + Jest suites | ✅ Fixed |
| BUG-013 | P2 | Repository Root | Inspect repository files | Clean source tree without nested apps or editor artifacts | Contained obsolete nested `my-app/`, backup `src/config/i18n.ts.bak`, `.aider*`, `.claude` | Lingering artifacts from initial setup and external tools | Purged `my-app/`, `i18n.ts.bak`, `.aider*`, `.claude`; updated `.gitignore` with `*.bak` and `*.orig` | Clean git status | ✅ Fixed |
| BUG-014 | P1 | Language Context `useLanguage` hook | Invoke `useLanguage` across app components | Adhere strictly to React Hook contracts without masking hook misuse in `try/catch` | `useLanguage` caught exceptions around `useContext` and returned fallback defaults, masking hook call errors | Wrapped `useContext(LanguageContext)` in defensive `try/catch` | Removed `try/catch` wrapper; directly returns `useContext(LanguageContext) \|\| DEFAULT_CONTEXT_VALUE` | `LanguageContext.test.ts` + `npx tsc --noEmit` | ✅ Fixed |
| BUG-015 | P2 | Leaderboard `/leaderboard` | View leaderboard as authenticated student | Display current student's actual ID and profile full name | Hardcoded fallback `usr_demo_001` and `You` | Did not read active `AuthSession` or `STUDENT_PROFILE` | Dynamically resolves `studentId` from `SessionRepository.getSession()` and `displayName` from `STUDENT_PROFILE` | Leaderboard suite + static checks | ✅ Fixed |
| BUG-016 | P2 | Home Barrel `src/features/home/index.ts` | Import Home 2.0 service/types from feature barrel | Direct access via `from '../features/home'` | `home2.service` and `home2.types` were not exported from feature barrel | Incomplete barrel index | Re-exported `home2.service` and `home2.types` from `src/features/home/index.ts` | `npx tsc --noEmit` + `npm test` | ✅ Fixed |

## Documented Notes

1. `STORAGE_KEYS` contains three alias constants that map to the same storage
   value (`LAST_PLAYED_GAME`/`GAMES_LAST_PLAYED`,
   `GAMES_DAILY_CHALLENGE`/`GAMES_DAILY_CHALLENGES`,
   `GAMES_BADGES`/`GAMES_UNLOCKED_BADGES`). Harmless; documented aliases.
2. Component-level hex colors match the canonical palette exactly (no off-palette/neon).
3. Nested `my-app/` starter scaffold, stale release zip archives, and all tool history files are fully purged from the repository.
4. Total Jest tests: 89 test suites, 1,410 tests passing cleanly in-band with zero failures.


